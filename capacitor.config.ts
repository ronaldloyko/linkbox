import { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "me.loyko.linkbox",
  appName: "Linkbox",
  webDir: "build",
  bundledWebRuntime: false,
};

export default config;

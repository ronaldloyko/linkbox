import { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "me.loyko.ronald.linkbox",
  appName: "Linkbox",
  webDir: "build",
  bundledWebRuntime: false,
};

export default config;
